[project]
name = "chore-button"
version = "0.1.0"
description = "Software that powers a button that will print out chores on receipt paper"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["flask>=3.1.3", "python-escpos>=3.1", "waitress>=3.0.2"]
